package entities;

import java.util.Date;

public class Camel extends PackAnimal {
    public Camel(String name, Date dateOfBirth, Integer maxLoad, Integer maxSpeed) {
        super(name, dateOfBirth, maxLoad, maxSpeed);
    }
}
