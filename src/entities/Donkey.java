package entities;

import java.util.Date;

public class Donkey extends PackAnimal{
    public Donkey(String name, Date dateOfBirth, Integer maxLoad, Integer maxSpeed) {
        super(name, dateOfBirth, maxLoad, maxSpeed);
    }
}
